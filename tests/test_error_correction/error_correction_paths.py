from os.path import join

from tests.files.paths import corrected_error_sol_path

corrected_error_solution_path = join(corrected_error_sol_path, 'corrected_errors_solution.csv')
phot_with_nan_corrected_sol_path = join(corrected_error_sol_path, 'phot_with_nan_corrected.csv')